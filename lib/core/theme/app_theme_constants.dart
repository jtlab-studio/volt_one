// lib/core/theme/app_theme_constants.dart
// This file contains constant theme values to replace dynamic theme provider

import 'package:flutter/material.dart';

// Constant theme values to use throughout the app
class AppThemeConstants {
  static const Color primaryColor = Colors.blue;
  static const Color accentColor = Colors.orange;
  static const Color navSelectedColor = Colors.blue;
  static const Color navUnselectedColor = Colors.grey;
  
  // Dark theme specific colors
  static const Color darkBackground = Color(0xFF121212);
  static const Color darkSurface = Color(0xFF1E1E1E);
  static const Color darkCard = Color(0xFF2C2C2C);
}
